<label for="avatarUrl">Avatar url:</label>
        <input class="textfield" type="text" id="avatarUrl" name="avatarUrl" maxlength="256"
        value=#if($avatarUrl) "$avatarUrl" #else "" #end />

<span>
  #if($avatarUrl)
      <img src="$avatarUrl" alt="">
  #end
</span>
